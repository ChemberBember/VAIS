# 🔒 Безопасная настройка Git

## ✅ Что уже сделано:

1. ✅ Обновлен `.gitignore` - исключает `config.py` и `config.json` с API ключами
2. ✅ Созданы примеры конфигов: `config.py.example` и `config.json.example`

## 🚀 Инструкция по первому пушу:

### 1. Проверьте, что API ключи не в конфигах

Убедитесь, что в `config.py` и `config.json` стоят заглушки:
- `"your_deepseek_api_key_here"` 
- НЕ реальные ключи!

### 2. Инициализируйте Git репозиторий

```bash
git init
```

### 3. Добавьте все файлы (config.py и config.json автоматически исключатся)

```bash
git add .
```

### 4. Проверьте, что config.py и config.json НЕ добавлены

```bash
git status
```

Вы НЕ должны видеть `config.py` и `config.json` в списке!

### 5. Сделайте первый коммит

```bash
git commit -m "Initial commit: Voice AI System"
```

### 6. Добавьте remote и запушьте

```bash
git remote add origin <ваш_репозиторий>
git branch -M main
git push -u origin main
```

## ⚠️ ВАЖНО: Если вы уже закоммитили config.py с реальным ключом

Если вы случайно закоммитили файл с реальным API ключом:

1. **Немедленно смените API ключ** на DeepSeek Platform!
2. Удалите ключ из истории Git:

```bash
# Удалить файл из истории
git filter-branch --force --index-filter "git rm --cached --ignore-unmatch config.py config.json" --prune-empty --tag-name-filter cat -- --all

# Или используйте BFG Repo-Cleaner (рекомендуется)
# https://rtyley.github.io/bfg-repo-cleaner/
```

3. Форсированно запушьте изменения:

```bash
git push origin --force --all
```

## 📝 Что попадет в Git:

✅ **Будет закоммичено:**
- `config.py.example` - пример без ключей
- `config.json.example` - пример без ключей
- Все остальные файлы проекта
- `.gitignore` - защита от случайного коммита

❌ **НЕ будет закоммичено:**
- `config.py` - ваш реальный конфиг с ключом
- `config.json` - ваш реальный конфиг с ключом
- `.venv/` - виртуальное окружение
- `.env` - переменные окружения

## 🔐 Рекомендации:

1. **Никогда не коммитьте реальные API ключи**
2. **Используйте примеры конфигов** для документации
3. **Храните реальные ключи локально** или в `.env` файле
4. **Если ключ попал в Git** - немедленно смените его!

## 📋 Чек-лист перед пушем:

- [ ] Проверил, что в `config.py` стоит `"your_deepseek_api_key_here"`
- [ ] Проверил, что в `config.json` стоит `"your_deepseek_api_key_here"`
- [ ] Запустил `git status` и убедился, что `config.py` и `config.json` не в списке
- [ ] Готов к безопасному пушу!

